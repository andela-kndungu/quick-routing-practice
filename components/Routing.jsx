import React from 'react';
import { Link } from 'react-router';

const Routing = () => (
  (
    <div>
      <h1>This is the routing page</h1>
      <h2>Use <Link to="/linked">this</Link> to navigate to another page</h2>
    </div>
  )
);

export default Routing;

